@using RepositoryFramework.Web.Components;
@inject AppSettings Settings

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
<link rel="stylesheet" href="_content/Radzen.Blazor/css/default-base.css">
<link rel="stylesheet" href="_content/Microsoft.AspNetCore.Components.QuickGrid/Microsoft.AspNetCore.Components.QuickGrid.bundle.scp.css">
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
<link href="_content/Rystem.RepositoryFramework.Web.Components/repository.css" rel="stylesheet" />

<style>
    :root {
        --primary: @(Settings.Palette.Primary);
        --secondary: @(Settings.Palette.Secondary);
        --success: @(Settings.Palette.Success);
        --info: @(Settings.Palette.Info);
        --warning: @(Settings.Palette.Warning);
        --danger: @(Settings.Palette.Danger);
        --light: @(Settings.Palette.Light);
        --dark: @(Settings.Palette.Dark);
        --link-color: @(Settings.Palette.Link.Color);
        --link-hover-color: @(Settings.Palette.Link.Hover);
        --btn-color: @(Settings.Palette.Button.Color);
        --btn-bg: @(Settings.Palette.Button.Background);
        --btn-hover-color: shade-color(@(Settings.Palette.Button.Color), 10%);
        --btn-hover-bg: shade-color(@(Settings.Palette.Button.Background), 15%);
        --bs-body-color: @(Settings.Palette.BackgroundColor);
        --table-color: @(Settings.Palette.Table.Color);
        --table-striped-color: @(Settings.Palette.Table.StripedColor);
        --table-bg: @(Settings.Palette.Table.Background);
        --table-striped-bg: @(Settings.Palette.Table.StripedBackground);
    }

    .table {
        --bs-table-color: var(--table-color);
        --bs-table-bg: var(--table-bg);
        --bs-table-border-color: var(--bs-border-color);
        --bs-table-striped-color: var(--table-striped-color);
        --bs-table-striped-bg: var(--table-striped-bg);
        color: var(--bs-table-color);
    }

    .form-control {
        min-height: 50px;
        font-size: @(Settings.Sizing.InputFontSize) !important;
    }

    input {
        font-size: @(Settings.Sizing.InputFontSize) !important;
    }

    .navbar-brand, .nav-item, .dropdown-item {
        font-size: @(Settings.Sizing.NavigationFontSize) !important;
    }

        .nav-item .active {
            font-weight: @(Settings.Sizing.NavigationFontWeight) !important;
        }

    .row {
        margin: 0px !important;
    }

    .dropdown-toggle::after {
        vertical-align: middle !important;
    }

    .table tbody > tr > td {
        vertical-align: middle;
    }

    .table-sticky th:first-child, .table-sticky td:first-child {
        position: sticky;
        left: 0px;
    }

    nav[b-3qssc0bm46] {
        float: right;
    }

    * {
        scrollbar-width: thin;
        scrollbar-color: var(--secondary) rgba(0,0,0,0);
    }

        *::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }

        *::-webkit-scrollbar-track {
            background: rgba(0,0,0,0);
        }

        *::-webkit-scrollbar-thumb {
            background-color: var(--secondary);
            border-radius: 10px;
            border: 3px solid rgba(0,0,0,0);
        }

    .col-options {
        min-width: 300px;
        margin-left: -50px;
    }

    .input-group-text {
        padding: 0px 10px !important;
    }
</style>